// Intentionally empty.
// This wrapper target exists so the XMediatorAppLovin product can combine the
// XMediatorAppLovin binary target and carry their required
// system-framework link settings (binary targets cannot declare either).
